# Port the HTTP server listens on. Optional, defaults to 8080.
PORT=8080

# Reserved for future Spotify/Last.fm integration (M3+). Not used yet —
# placeholders only, never commit real values here.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_REDIRECT_URI=
LASTFM_API_KEY=
